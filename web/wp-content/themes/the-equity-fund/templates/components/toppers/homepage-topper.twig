<header class="palette-lavender bg-background px-site-padding pb-12 md:pb-16 pt-topper-margin size-{{ size|default('large') }}">
  <div class="relative">

    <figure class="w-full xl:w-[80%] mx-auto relative">

      {# MOBILE #}
      {% if image_mobile %}
        <div class="[display:block] md:[display:none] w-full">
          {% include 'templates/components/lazy-img.twig' with {
            img: get_image(image_mobile),
            aspectRatio: 'horizontal',
          } %}
        </div>
      {% endif %}

      {# DESKTOP #}
      {% if image %}
        <div class="[display:none] md:[display:block] w-full">
          {% include 'templates/components/lazy-img.twig' with {
            img: get_image(image),
            aspectRatio: 'wide',
          } %}
        </div>
      {% endif %}

    </figure>

    <h1 class="w-full font-display text-yellow font-medium text-center uppercase leading-[1] text-[20vw] sm:text-[18vw] md:text-[16vw] absolute left-1/2 -translate-x-1/2 top-full -translate-y-1/2">
        {{ headline }}
    </h1>
  </div>

  <div class="text-foreground mt-[4vw] pt-14 sm:pt-16 md:pt-18 lg:pt-20 xl:pt-24 text-center">
      <p class="text-base md:text-lg lg:text-xl xl:text-2xl font-serif mx-auto max-w-[50rem]">
          {{ introduction }}
      </p>
  </div>
</header>
